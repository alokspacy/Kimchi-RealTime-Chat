# Workspace Rules

- **Git & GitHub Commands**: You must always ask the user for explicit confirmation/permission before executing any `git` or `github` related commands in the terminal. Do not run git actions autonomously.
